PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/qa_portfolio
CORS_ORIGIN=http://localhost:5173

# Cloudinary credentials - set these in your deployment environment (Render dashboard)
# Do NOT commit real secrets to source control. Example:
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# When deploying, set CORS_ORIGIN to your frontend origins, comma-separated. Example:
# CORS_ORIGIN=https://lifeofthecoders.github.io,https://yourdomain.com
